name: Diagnose atlas figure candidates

on:
  workflow_dispatch:
  push:
    paths:
      - 'diagnose_figures.py'
      - '.github/workflows/diagnose-atlas.yml'

permissions:
  contents: read

env:
  PYTHONUNBUFFERED: '1'

jobs:
  diagnose:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
          cache-dependency-path: requirements-atlas.txt
      - name: Install dependencies
        run: pip install -r requirements-atlas.txt
      - name: Print candidate figure captions
        run: python diagnose_figures.py
